# HANDOVER

最終更新: 2026-09-16 / v1.1.1 / main / 作業ツリー: 未コミット3件（`CLAUDE.md` 新規・`HANDOVER.md` 新規・`.github/workflows/deploy.yml` 修正）

## Goal

公開キットから案件を特定できる情報を排除しきり、二度と混入しない仕組みを入れる。あわせてこのリポジトリの恒久情報とセッション情報を2層に整える。

## 直近でやったこと

- 公開リポジトリを作り直した。初代を `-public-archive` に改名して private 化し、同名で新しい public を作成。履歴・タグ4本・Release 3本を復元した（削除は0件）
- コミットメッセージを `git filter-repo` で書き換え、README と `examples/` のオーナー名を `your-org` に置換した
- `.githooks/` に案件名を弾く pre-commit / commit-msg を追加し、動作確認した
- `CLAUDE.md` と `HANDOVER.md` を新規作成し、`deploy.yml` 冒頭コメントの `secrets: inherit` を明示指定に直した

## 決めたこと（と理由）

- **リポジトリを削除せず、改名＋private 化で退役させた** … `delete_repo` スコープ不要で、記録が残り、元に戻せる
- **コミットメッセージのみ書き換え、ファイルの中身は書き換えなかった** … 中身を変えると `v1.1.1` の tree が変わり、案件側の `package-lock.json` の integrity が壊れて `npm ci` が落ちる。クライアント名はファイルには一度も入っていなかったため、これで目的を達している
- **禁止語リストはリポジトリ外に置いた** … リストをコミットしたら、それ自体が公開になる
- **バージョンは上げない** … 配布される挙動は変わっていない（README とフックの追加のみ）

## 避けること

- **`git push -f` や PR 本文の編集で案件名を消そうとしない。** `refs/pull/*` は消せず、本文の編集は編集履歴が残る。リポジトリごと退役させるしかない
- **`grep -v '^\+\+\+'` と書かない。** BRE では `\+` が繰り返し演算子になり全行が消える。`-E` を付ける
- 本番サーバーへの SSH 読み取り、private リポジトリのファイル読み取りは自動承認で止まる。**代替手段を探すより、ユーザーに実行してもらう方が速い**
- `gh release create` を JSON 読み取りと同じ複合コマンドにまとめると弾かれる。**1コマンド1操作に分ける**と通る

## 待ち（こちらは動けない）

- **案件側の `npm ci` が通るかの実測。** 新しい tarball の sha512 は `sha512-J1tLgOikw8yFKC1LBSgSRwr3RmJDoAU7AcXXwpmjql1/ZAwIpUamWdF67BPvtTHMNhsKn2KX3Bhs+1UTYxKarA==`。案件の `package-lock.json` の `integrity` と一致すれば無傷、違えば案件側で `npm i` を流し直す
- **次の導入予定案件はテーマ側の作業が途中**のため保留。調査結果はユーザーのメモリに退避済み

## 最初の一手

`/land` で未コミットの3件を main に載せる（バージョンは上げない。タグも Release も作らない）。

## 深掘りするなら

- このリポジトリの恒久ルール … `CLAUDE.md`
- 利用者向けの全手順・落とし穴の表 … `README.md`
- 保留案件の調査結果・リポジトリ構成の経緯 … ユーザーのメモリ（`MEMORY.md` から辿る）
