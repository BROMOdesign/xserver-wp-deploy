# HANDOVER

最終更新: 2026-09-16 / v1.1.1 / main / 作業ツリー: clean

## Goal

キット本体の整備、履歴書き換えの後始末、導入済み案件の初回デプロイまで完了。**着手すべき残件は無い。**

## 直近でやったこと

- 公開リポジトリを作り直し、コミットメッセージとファイルから案件名を排除（PR #1 / #2 マージ済み）
- `.githooks/` に案件名を弾くフックを追加。`CLAUDE.md` / `HANDOVER.md` の2層を新設
- **`/xwp-setup` を作成**（`~/.claude/commands/xwp-setup.md`、299行）。キット導入の起点をコマンド1本に固定した
- **★ 履歴書き換えが tarball のハッシュを変えることを実測で確認**し、`CLAUDE.md` に恒久ルールとして追記
- 影響を受けていた先行案件でキットを `v1.1.1` に上げ、lock の `integrity` を更新（**空キャッシュでの `npm ci` 成功まで確認**）。その案件の `develop` を `main` に早送りで追従
- **もう1案件の初回デプロイが完了**（別セッション）。配信アセットが更新され、露出していたソースマップが 404 になったことを HTTP で確認

## 決めたこと（と理由）

- **リポジトリを削除せず、改名＋private 化で退役させた** … `delete_repo` スコープ不要で、記録が残り、元に戻せる
- **ファイルの中身は書き換えず、コミットメッセージだけ直した** … クライアント名はファイルに一度も入っていなかったため、これで目的を達している
- **禁止語リストはリポジトリ外（`~/.config/git/private-names.txt`）に置いた** … コミットしたらそれ自体が公開になる
- **`v1` を動かしていない** … 配布される挙動は変わっていない
- **導入の起点を `/xwp-setup` に固定した** … 始点が曖昧だと同じ作業のブランチが複数生まれる（実際に2本生まれた）

## 避けること

- **「tree が同じなら tarball も同じ」と考えない。** `pax_global_header` にコミット SHA が入るため、履歴書き換えで必ずハッシュが変わる。**このセッションで一度これを読み違えた**
- **`git push -f` や PR 本文の編集で案件名を消そうとしない。** `refs/pull/*` は消せず、本文編集は編集履歴が残る
- **`grep -v '^\+\+\+'` と書かない。** BRE では `\+` が繰り返し演算子になり全行が消える。`-E` を付ける
- **長い Markdown をヒアドキュメントで書かない。** bash が構文エラーになる。`Write` ツールを使う
- **並行セッションが走っていると状態が動く。** 案件側の状態は「数分前のスナップショット」として扱い、断定しない
- **`npm ci` が通ったことを「壊れていない」の根拠にしない。** ローカルキャッシュに旧 tarball が残っていると通ってしまう。`npm ci --cache "$(mktemp -d)"` で確かめる。**このセッションで一度これに騙された**
- 次は自動承認で止まる。**代替手段を探すよりユーザーに打ってもらう方が速い** … 本番サーバーへの SSH、private リポジトリのファイル読み取り、`gh pr merge`

## 待ち（こちらは動けない）

- 無し。前回の「本番のドキュメントルートの実測」は、別セッションでのデプロイ完了をもって解消した

## 最初の一手

無し。キット本体に残件は無い。次の案件にキットを入れるときは `/xwp-setup` から始める（Phase 0 の調査で、その案件に不要な手順が先に分かる）。

## 深掘りするなら

- このリポジトリの恒久ルール … `CLAUDE.md`
- 利用者向けの全手順・落とし穴の表 … `README.md`
- 案件へのキット導入の進め方 … `~/.claude/commands/xwp-setup.md`
- リポジトリ構成の経緯・案件別の調査結果 … ユーザーのメモリ（`MEMORY.md` から辿る）
